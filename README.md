# MBTI 性格测试网站

一个精美的 MBTI 性格测试网站，基于 Vite + Vanilla JS 构建，纯前端实现，可直接部署到 Cloudflare Pages 等静态站点托管服务。

## 项目特性

- ✨ 移动端优先的响应式设计
- 🌓 支持暗色模式（跟随系统）
- 🎯 60 道精心设计的题目
- 💾 本地进度保存与恢复
- 📊 详细的性格维度分析
- 🎨 现代化的 UI 设计，包含平滑动画
- ♿ 良好的可访问性支持
- 🚀 纯静态站点，无需后端

## 技术栈

- **构建工具**: Vite 5.0
- **前端框架**: Vanilla JS (原生 JavaScript)
- **样式**: CSS3 (支持暗色模式)
- **模块化**: ESM (ECMAScript Modules)
- **路由**: Hash 路由
- **存储**: localStorage

## 项目结构

```
mbti-test/
├── index.html              # HTML 入口文件
├── package.json            # 项目配置
├── vite.config.js          # Vite 配置
├── README.md               # 项目文档
├── public/
│   └── favicon.svg         # 网站图标
└── src/
    ├── main.js             # 主入口文件
    ├── router.js           # 路由模块
    ├── store.js            # 状态管理 (localStorage)
    ├── questions.js        # 题库模块 (60道题)
    ├── scoring.js          # 计分模块
    ├── results.js          # 结果数据 (16种类型)
    ├── styles.css          # 全局样式
    └── pages/
        ├── home.js         # 首页
        ├── test.js         # 测试页
        ├── result.js       # 结果页
        └── about.js        # 关于页
```

## 本地运行

### 安装依赖

使用 npm:
```bash
npm install
```

使用 pnpm:
```bash
pnpm install
```

使用 yarn:
```bash
yarn install
```

### 启动开发服务器

```bash
npm run dev
```

或

```bash
pnpm dev
```

开发服务器将在 `http://localhost:3000` 启动。

### 构建生产版本

```bash
npm run build
```

或

```bash
pnpm build
```

构建完成后，生成的文件将位于 `dist/` 目录。

### 预览生产版本

```bash
npm run preview
```

或

```bash
pnpm preview
```

## 部署到 Cloudflare Pages

### 方法一：通过 Git 仓库部署

1. 将项目推送到 GitHub/GitLab/Bitbucket
2. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
3. 进入 Pages 页面，点击 "Create a project"
4. 选择你的 Git 仓库
5. 配置构建设置：
   - **Build command**: `npm run build` 或 `pnpm build`
   - **Build output directory**: `dist`
6. 点击 "Save and Deploy"

### 方法二：通过 Direct Upload 部署

1. 在本地构建项目：
   ```bash
   npm run build
   ```

2. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
3. 进入 Pages 页面，点击 "Create a project"
4. 选择 "Direct Upload"
5. 上传 `dist/` 目录中的所有文件
6. 点击 "Deploy site"

### 方法三：使用 Wrangler CLI

1. 安装 Wrangler CLI：
   ```bash
   npm install -g wrangler
   ```

2. 构建项目：
   ```bash
   npm run build
   ```

3. 部署：
   ```bash
   wrangler pages deploy dist
   ```

## 功能说明

### 页面结构

1. **首页** (`#/home`)
   - 测试介绍
   - 开始测试按钮
   - 继续上次测试（如有未完成进度）
   - 免责声明入口

2. **测试页** (`#/test`)
   - 单题卡片式答题
   - 5 级 Likert 选项
   - 实时进度显示
   - 上一题/下一题导航
   - 退出并保存功能

3. **结果页** (`#/result`)
   - MBTI 类型展示
   - 4 维度百分比条形图
   - 类型详细描述
   - 优势、盲点、建议
   - 重新测试、分享功能

4. **关于页** (`#/about`)
   - MBTI 理论介绍
   - 四个维度说明
   - 免责声明
   - 常见问题

### 计分规则

- 每题映射到一个维度：EI / SN / TF / JP
- 每题包含 polarity（1 或 -1），表示倾向方向
- Likert 5 级选择映射为数值 1-5
- 根据 polarity 和答案值计算维度得分
- 将得分转换为百分比
- 最终类型取每个维度得分较高的一侧

### 数据存储

- 使用 localStorage 保存测试进度
- 保存内容：当前题号、用户答案、题目顺序
- 支持继续未完成的测试
- 重新测试时会清除所有缓存

## 浏览器支持

- Chrome (最新版)
- Firefox (最新版)
- Safari (最新版)
- Edge (最新版)
- 移动端浏览器 (iOS Safari, Chrome Mobile)

## 开发说明

### 添加新题目

编辑 `src/questions.js` 文件，按照现有格式添加新题目：

```javascript
{
  id: "q61",
  text: "题目内容",
  dimension: "EI",  // EI / SN / TF / JP
  polarity: 1       // 1 或 -1
}
```

### 修改类型描述

编辑 `src/results.js` 文件，修改对应类型的描述、优势、盲点、建议。

### 自定义样式

编辑 `src/styles.css` 文件，修改 CSS 变量来自定义颜色、字体、间距等。

## 注意事项

- 本测试仅供娱乐和自我探索参考
- 不能替代专业的心理评估或诊断
- 请勿将测试结果用于医疗诊断、雇佣决策等
- 性格是复杂且多面的，MBTI 只是理解自己的一种工具

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！

## 联系方式

如有问题或建议，请通过 Issue 联系。